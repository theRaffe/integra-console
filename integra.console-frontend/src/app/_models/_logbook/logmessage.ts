export class LogMessage {
    logId: number;
    processId: string;
    businessMsgId: string;
    processName: string;
    subprocess: string;
    creationDate: Date;
    processIdentifier: string;
}